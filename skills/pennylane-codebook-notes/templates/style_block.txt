<style>
  :root {
    --bg: #faf8f3;
    --surface: #ffffff;
    --ink: #1e2024;
    --ink-soft: #55585f;
    --ink-faint: #8a8d94;
    --accent: #2c4a85;
    --accent-soft: #eef1f8;
    --rule: #ddd6c6;
    --exercise: #a3691a;
    --exercise-ink: #7a4e10;
    --exercise-bg: #fdf6e7;
    --exercise-border: #e6cd9a;
    --solution: #2f7a4d;
    --solution-ink: #245c3a;
    --solution-bg: #eef6f0;
    --note-accent: #a44944;
    --note-bg: #fbecea;
    --code-bg: #eef0ea;
  }
  @media (prefers-color-scheme: dark) {
    :root {
      --bg: #15171c;
      --surface: #1b1e25;
      --ink: #e9eaee;
      --ink-soft: #aeb1ba;
      --ink-faint: #797d87;
      --accent: #8ca4de;
      --accent-soft: #202b42;
      --rule: #33363f;
      --exercise: #e0a94e;
      --exercise-ink: #f0c274;
      --exercise-bg: #2a2415;
      --exercise-border: #4a3b1c;
      --solution: #74c294;
      --solution-ink: #8fd4ac;
      --solution-bg: #172420;
      --note-accent: #d97b76;
      --note-bg: #2a1c1b;
      --code-bg: #262a25;
    }
  }
  :root[data-theme="dark"] {
    --bg: #15171c; --surface: #1b1e25; --ink: #e9eaee; --ink-soft: #aeb1ba; --ink-faint: #797d87;
    --accent: #8ca4de; --accent-soft: #202b42; --rule: #33363f;
    --exercise: #e0a94e; --exercise-ink: #f0c274; --exercise-bg: #2a2415; --exercise-border: #4a3b1c;
    --solution: #74c294; --solution-ink: #8fd4ac; --solution-bg: #172420;
    --note-accent: #d97b76; --note-bg: #2a1c1b; --code-bg: #262a25;
  }
  :root[data-theme="light"] {
    --bg: #faf8f3; --surface: #ffffff; --ink: #1e2024; --ink-soft: #55585f; --ink-faint: #8a8d94;
    --accent: #2c4a85; --accent-soft: #eef1f8; --rule: #ddd6c6;
    --exercise: #a3691a; --exercise-ink: #7a4e10; --exercise-bg: #fdf6e7; --exercise-border: #e6cd9a;
    --solution: #2f7a4d; --solution-ink: #245c3a; --solution-bg: #eef6f0;
    --note-accent: #a44944; --note-bg: #fbecea; --code-bg: #eef0ea;
  }
  * { box-sizing: border-box; }
  body {
    font-family: Georgia, 'Noto Serif KR', 'Nanum Myeongjo', serif;
    line-height: 1.75;
    color: var(--ink);
    background: var(--bg);
    max-width: 760px;
    margin: 0 auto;
    padding: 56px 28px 100px;
  }
  h1, h2, h3, h4 { text-wrap: balance; font-weight: 700; }
  h1 {
    font-size: 32px;
    letter-spacing: -0.01em;
    margin: 0 0 6px;
    color: var(--ink);
  }
  .eyebrow {
    font-family: -apple-system, 'Helvetica Neue', sans-serif;
    font-size: 11.5px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--accent);
    font-weight: 600;
    margin-bottom: 14px;
  }
  h2 {
    font-size: 23px;
    margin: 64px 0 4px;
    padding-bottom: 10px;
    border-bottom: 2px solid var(--accent);
    color: var(--ink);
    display: flex;
    align-items: baseline;
    gap: 10px;
  }
  h2 .num {
    font-family: -apple-system, 'Helvetica Neue', sans-serif;
    font-size: 14px;
    font-weight: 700;
    color: var(--accent);
    letter-spacing: 0.02em;
  }
  h3 { font-size: 18.5px; margin: 36px 0 10px; color: var(--ink); }
  h4 { font-size: 15.5px; margin: 22px 0 8px; color: var(--ink-soft); }
  p { font-size: 15.5px; margin: 12px 0; text-align: justify; color: var(--ink); }
  ul, ol { font-size: 15.5px; padding-left: 22px; }
  li { margin: 4px 0; }
  a { color: var(--accent); }
  a:focus-visible, summary:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
  .subtitle { font-size: 16px; color: var(--ink-soft); margin-top: 2px; }
  .meta {
    font-family: -apple-system, 'Helvetica Neue', sans-serif;
    font-size: 12.5px; color: var(--ink-faint); margin-top: 16px; letter-spacing: 0.01em;
  }
  .goalbox {
    background: var(--accent-soft); border-left: 4px solid var(--accent);
    padding: 16px 22px; margin: 20px 0; border-radius: 2px;
  }
  .goalbox p { margin: 4px 0 8px; font-size: 13.5px; font-weight: 600; letter-spacing: 0.03em; text-transform: uppercase; color: var(--accent); font-family: -apple-system, 'Helvetica Neue', sans-serif; }
  .goalbox ul { margin: 6px 0 2px; }
  .goalbox li { font-size: 15px; }
  .exbox {
    background: var(--exercise-bg); border: 1px solid var(--exercise-border); border-left: 4px solid var(--exercise);
    padding: 16px 22px; margin: 22px 0; border-radius: 2px;
  }
  .exbox .extitle {
    font-family: -apple-system, 'Helvetica Neue', sans-serif;
    font-weight: 700; font-size: 12.5px; letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--exercise-ink); margin-bottom: 8px;
  }
  .solbox {
    background: var(--solution-bg); border-left: 3px solid var(--solution);
    padding: 12px 20px; margin: 14px 0 4px; border-radius: 2px;
  }
  .solbox .soltitle {
    font-family: -apple-system, 'Helvetica Neue', sans-serif;
    font-weight: 700; font-size: 12px; letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--solution-ink); margin-bottom: 6px;
  }
  .figure { text-align: center; margin: 26px 0; }
  .figure img { max-width: 100%; background: #fff; border-radius: 3px; }
  .figure .cap { font-size: 12.5px; color: var(--ink-faint); margin-top: 8px; font-style: italic; }
  .formula { text-align: center; margin: 16px 0; overflow-x: auto; }
  .formula img { max-width: 100%; background: #fff; padding: 2px; border-radius: 2px; }
  .note {
    background: var(--note-bg); border-left: 4px solid var(--note-accent);
    padding: 12px 20px; margin: 16px 0; font-size: 14.5px; border-radius: 2px; color: var(--ink);
  }
  code {
    background: var(--code-bg); padding: 1px 6px; border-radius: 3px; font-size: 13.5px;
    font-family: 'SF Mono', Menlo, monospace;
  }
  .divider { border: none; border-top: 1px solid var(--rule); margin: 52px 0; }
  table.summary {
    border-collapse: collapse; width: 100%; margin: 18px 0; font-size: 13.5px;
    font-variant-numeric: tabular-nums;
  }
  table.summary th, table.summary td { border: 1px solid var(--rule); padding: 9px 11px; text-align: left; }
  table.summary th {
    background: var(--accent-soft); color: var(--accent);
    font-family: -apple-system, 'Helvetica Neue', sans-serif;
    font-size: 11.5px; letter-spacing: 0.05em; text-transform: uppercase; font-weight: 700;
  }
  table.summary td:nth-child(2) { font-family: 'SF Mono', Menlo, monospace; font-size: 12.5px; }
  .toc {
    background: var(--surface); border: 1px solid var(--rule);
    padding: 18px 26px; margin: 24px 0; border-radius: 3px;
  }
  .toc b {
    font-family: -apple-system, 'Helvetica Neue', sans-serif;
    font-size: 12px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--ink-faint);
  }
  .toc ul { margin: 8px 0 0; padding-left: 20px; }
  .toc li { font-size: 14.5px; margin: 5px 0; }
</style>
